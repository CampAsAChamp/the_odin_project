export default function Cart() {
  return (
    <div>
      <h1>Cart</h1>
      {/* Display each item here and add an input field on it to allow the user to input however many they want
      buttons to it to adjust the quantity */}
      <h1>Total:{}</h1>
      <button>Checkout</button>
    </div>
  );
}
